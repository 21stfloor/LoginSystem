// const dotenv = require('dotenv');

// const loadEnvVariables = () => {
//     dotenv.config(); // Load variables from default .env file
  
//     if (process.env.NODE_ENV === 'production') {
//       dotenv.config({ path: 'production.env' }); // Load production variables
//     } else {
//       dotenv.config({ path: 'development.env' }); // Load development variables
//     }
// };

// module.exports = loadEnvVariables;